"""Stub de compatibilidad — Ley de Benford por candidato.

La implementación fue consolidada en `benford_first_digit_rule.py`
para eliminar la duplicación de lógica Benford. Este módulo se mantiene
vacío para que el import explícito en `rules_engine.py` siga funcionando
sin modificar la lista de importaciones.

Compatibility stub — Benford's Law per candidate.

The implementation was consolidated into `benford_first_digit_rule.py`
to eliminate duplicated Benford logic. This module is kept empty so that
the explicit import in `rules_engine.py` continues to work without
modifying the import list.
"""

# Registration of config_key="benford_law" happens in benford_first_digit_rule.py.
