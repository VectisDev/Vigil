# ES: Flags de extensiones opcionales para el núcleo congelado.
# EN: Optional extension flags for the frozen core.
ENABLE_CLOUDFLARE: false  # DNS-over-HTTPS fallback only — no account needed
ENABLE_UPNFM: false
